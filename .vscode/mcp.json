{
    "servers": {
        "specrail": {
            "type": "stdio",
            "command": "${workspaceFolder}/target/debug/specrail",
            "env": {
                // "SPECRAIL_MCP_DEBUG_LOG": "/tmp/specrail-mcp-debug.log",
                // "SPECRAIL_MCP_DEBUG_STDERR": "0"
            },
            "args": [
                "mcp-server"
            ]
        }
    }
}